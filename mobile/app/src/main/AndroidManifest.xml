<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.praeter">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Location -->
    <!--For using only network based location use this one-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--ACCESS_FINE_LOCATION permission need for google play services version.-->
    <!-- For GPS based location, this one -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- User Data -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />


    <!-- Hardware -->
    <uses-permission android:name="android.permission.VIBRATE" />


    <application
        android:name=".PraeterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_praeter_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_praeter_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Praeter">
        <activity
            android:name=".ui.splashscreen.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.servicepicker.ServicePickerActivity"
            android:theme="@style/Theme.Praeter.NoActionBar" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity android:name=".ui.mainactivity.MainActivity" />

    </application>

</manifest>